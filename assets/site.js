const b=document.querySelector('.menu-button');b?.addEventListener('click',()=>{const o=document.body.classList.toggle('menu-open');b.setAttribute('aria-expanded',String(o))});document.querySelectorAll('.lead-form').forEach(form=>form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const phone=String(data.get('phone')||'').trim();const email=String(data.get('email')||'').trim();const status=form.querySelector('.form-status');if(!phone&&!email){status.textContent='Укажите телефон или e-mail.';return}const subject='Запрос расчёта — '+(form.dataset.source||'сайт');const body=['Имя: '+(data.get('name')||''),'Компания: '+(data.get('company')||''),'Телефон: '+phone,'E-mail: '+email,'','Задача:',''+(data.get('task')||''),'','Страница: '+location.href,'','Важно: приложите выбранные файлы к письму вручную.'].join('\n');status.textContent='Открываем письмо. Добавьте выбранные файлы во вложения.';location.href='mailto:savkinayus@wolfagro.ru?subject='+encodeURIComponent(subject)+'&body='+encodeURIComponent(body)}));document.querySelectorAll('a[href^="tel:"]').forEach(a=>a.addEventListener('click',()=>window.dataLayer?.push({event:'lead_phone_click'})));document.querySelectorAll('a[href^="mailto:"]').forEach(a=>a.addEventListener('click',()=>window.dataLayer?.push({event:'lead_email_click'})));